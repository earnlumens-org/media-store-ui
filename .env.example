# Copy this file to `.env.development.local` (or set vars in your hosting)
# Vite exposes only variables prefixed with VITE_

# Base URL for backend API (auth endpoints live here too)
# If left empty, the app auto-detects based on hostname:
#   - localhost / 127.0.0.1      -> http://localhost:8080
#   - app-dev.earnlumens.org     -> https://api-dev.earnlumens.org
#   - earnlumens.org             -> https://api.earnlumens.org
# Set this only if you need to override the auto-detection.
VITE_API_BASE_URL=
